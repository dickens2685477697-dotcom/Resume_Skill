# Agent Instructions

## 项目定位

本仓库维护 `resume-tailor`：一个基于真实材料建立可追溯证据库，并据此完成岗位分析、经历匹配、定向简历生成和事实审计的 Codex Skill。

## 必读文件与职责边界

- 执行 Skill，或修改其运行行为、模板、脚本和数据契约前，先阅读 `resume-tailor/SKILL.md`，再按涉及的阶段读取相关参考文件；无需一次性加载全部参考。
- `README.md` 是面向使用者的安装和使用文档。
- `resume-tailor/assets/templates/` 定义结构化数据模板；`resume-tailor/scripts/` 负责初始化、校验和确定性更新；`resume-tailor/tests/` 覆盖关键工作流。
- 用户的真实简历、JD、个人资料及其生成结果应保存在仓库外的 Workspace。若当前任务目录就是本仓库，不要默认在仓库内创建个人资料 `workspace/`；优先使用已有的仓库外指定路径，没有可用路径时再询问。
- 测试使用临时目录和明确标注的合成数据；仓库可保存必要的合成测试夹具，不使用真实个人资料充当夹具。

## 执行与完成条件

- 区分维护 Skill 与实际处理简历：维护仓库时不要触发个人材料导入或初始化个人 Workspace。
- 在用户已授权的范围内完成实施和必要验证。非关键缺口不阻塞可独立完成的工作；已明确的授权和答案无需重复确认。
- 可以对实现细节作合理假设，并说明影响结果的假设；不得对个人经历、职责或成果作事实假设。缺少证据时完成有依据的部分，并记录缺口。
- 若缺失信息会影响事实准确性、关键范围或不可逆操作，只询问必要问题，同时继续不依赖答案的工作。
- 保留仓库已有的未提交修改，不覆盖、回退或清理与当前任务无关的改动。
- 交付时简述改动、实际完成的验证及未解决问题；未运行的检查不得声称通过。

## 不可违反的规则

1. 不编造经历、职责、数字、成果、工具、团队规模或上线状态；无法确认的信息必须保留证据状态并列入缺口。
2. 区分个人贡献与团队成果，区分目标/计划指标与已经发生的结果。
3. 保留用户原始材料和来源引用，不覆盖或删除原件；更新 Workspace 中的结构化记录时保持幂等并保留变更痕迹。这不禁止修改任务范围内的仓库源码、模板和文档。
4. 更新 Workspace JSON 时，优先使用覆盖该阶段的现有脚本；岗位分析和匹配按对应 staging 流程执行。其他阶段遵循各自的数据契约和写入要求，不将匹配流程套用到所有 JSON。仓库模板可直接编辑，并按影响范围校验。
5. 保持 Python 3.10+ 兼容，并使用 UTF-8。行为或数据契约变化时，同步更新受影响的文档和必要测试。

## 修改后的检查

- 纯文档或指令修改：检查路径、命令、规则一致性和改动差异，无需为措辞修改新增测试或运行全套测试。
- 脚本行为或数据契约修改：运行相关测试；涉及共享校验器或跨阶段流程时，运行完整测试集：

```bash
python3 -m unittest discover -s resume-tailor/tests -p 'test_*.py'
```

- 创建或更新 Workspace 数据后，校验对应 Workspace；验证模板或初始化逻辑时使用临时测试目录，不为检查创建真实个人资料：

```bash
python3 resume-tailor/scripts/validate_workspace.py --root <workspace-path>
```

必要检查通过后，除非出现新改动、失败或未解决的具体问题，不重复扩大测试范围。交付前检查 `git diff` 和 `git status`；对尚未跟踪的文件直接检查内容，确认没有意外的个人材料、生成物或大范围改动。
